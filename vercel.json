{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "outputDirectory": "dist",
  "framework": "vite",
  "env": {
    "GEMINI_API_KEY": {
      "description": "Google Generative AI API Key for the research functionality",
      "required": true
    }
  }
}
